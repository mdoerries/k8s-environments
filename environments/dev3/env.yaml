# --- Identitaet (gelesen vom ApplicationSet-Generator) ---
name: dev3
creator: platform@example.com
purpose: Verprobt Overrides - web-a auf Chart 0.2.0 gepinnt, web-b abgeschaltet

# --- Deployment-Config (gelesen vom app-of-apps Chart) ---
envRevision: main
componentOverrides:
  - name: web-a
    version: 0.2.0
  - name: web-b
    enabled: false
